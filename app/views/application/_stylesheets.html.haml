= stylesheet_link_tag 'application', :media => nil
= stylesheet_link_tag params[:controller], :media => nil if Rails.application.assets.find_asset params[:controller]

-# Append your own using content_for :stylesheets
= yield :stylesheets
